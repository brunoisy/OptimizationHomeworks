function [ x ] = smoothedGradientAcc(A, b, lambda, x0, N, epsilon)
%SMOOTHEDGRADIENTACC Summary of this function goes here
%   Detailed explanation goes here

n = length(x0);
mu = epsilon/lambda;
L = 2*max(eig(A'*A))+1/mu;

h = 1/L;
gradF = makeGradSmoothedF(A, b, lambda, mu);
x = zeros(n,N+2);
x(:,1) = x0;
x(:,2) = x0;
for i = 1:N
    beta = 
end

x = x(2:end); % to remove x(-1)

end

