function [ x ] = smoothedGradient(A, b, lambda, x0, N, epsilon)
%UNTITLED Summary of this function goes here
%   Detailed explanation goes here

n = length(x);
mu = epsilon/lambda;
L = 2*max(eig(A^TA))+1/mu;

h = 1/L;
gradF = makeGradSmoothedF(A, b, lambda, mu);
x = zeros(
for i = 1:N
    x(i+1)
end

